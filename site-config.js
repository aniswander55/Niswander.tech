/**
 * Niswander Tech — shared site config (load before app scripts on product pages).
 * Override URLs per environment when deploying.
 */
window.SALESVIBE_APP_URL = window.SALESVIBE_APP_URL || 'https://salesai-2cd3e.web.app';
window.JOBS_APP_URL = window.JOBS_APP_URL || 'https://jobsearch-e9866.web.app';
